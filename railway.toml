import telebot
from telebot import types

TOKEN = "8615393508:AAGJrAkoXajxR11_rdcjJ9L5gEmicrBouAo"

bot = telebot.TeleBot(8615393508:AAGJrAkoXajxR11_rdcjJ9L5gEmicrBouAo)

def main_menu(chat_id):
    markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
    btn1 = types.KeyboardButton("💰 Купить")
    btn2 = types.KeyboardButton("ℹ️ О нашем боте")
    btn3 = types.KeyboardButton("📚 Тутор")
    markup.add(btn1)
    markup.add(btn2)
    markup.add(btn3)

    bot.send_message(chat_id, "Выберите действие:", reply_markup=markup)

@bot.message_handler(commands=['start'])
def start(message):
    main_menu(message.chat.id)

@bot.message_handler(func=lambda message: True)
def buttons(message):
    chat_id = message.chat.id

    if message.text == "💰 Купить":
        markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
        markup.add("✅ Подтвердить")

        bot.send_message(
            chat_id,
            "💳 Для покупки используйте сайт:\n\n"
            "🔗 Ссылка: https://share.google/4V5mm2EY0bkPLltax\n"
            "🆔 ID для оплаты: 269188645\n\n"
            "120руб.",
            reply_markup=markup
        )

    elif message.text == "✅ Подтвердить":
        markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
        markup.add("❌ Отмена")

        bot.send_message(
            chat_id,
            "📌 Отправьте скрин оплаты после покупки.\n"
            "Скрипт будет выдан в течение 60 минут.\n"
            "Если оплаты не было — скрипт выдан не будет.",
            reply_markup=markup
        )

    elif message.text == "❌ Отмена":
        bot.send_message(chat_id, "Возврат в главное меню.")
        main_menu(chat_id)

    elif message.text == "ℹ️ О нашем боте":
        bot.send_message(
            chat_id,
            "🤖 Информация о боте\n\n"
            "Владелец: @ring9ord\n"
            "Telegram канал: @ring9script"
        )

    elif message.text == "📚 Тутор":
        bot.send_message(
            chat_id,
            "📖 Туториал:\nhttps://t.me/ring9script"
        )

bot.infinity_polling()
